[workspace]
resolver = "2"
members = [
    "crates/mere",
    "crates/canvas/canvas",
    "crates/canvas/cartography",
    "crates/canvas/pictograph",
    "crates/graph/glossary",
    "crates/graph/subgraph",
    "crates/graph/graph-kernel",
    "crates/graph/linked-data",
    "crates/shell/chrome",
    "crates/shell/comms",
    "crates/incipit",
    "crates/capability",
    "crates/system/pandect",
    "crates/system/resident",
    "crates/system/luggage",
    "crates/system/fetch",
    "crates/system/content-contract",
    "crates/system/proofs",
    "crates/system/shell-state",
    "crates/system/notochord",
    "crates/system/registry/register-diagnostics",
    "crates/system/registry/register-input",
    "crates/system/registry/register-knowledge",
    "crates/system/registry/register-layout",
    "crates/system/registry/register-lens",
    "crates/system/registry/register-mod-loader",
    "crates/system/registry/register-protocol",
    "crates/system/registry/register-theme",
    "crates/system/registry/register-viewer",
    "crates/system/ux-events",
    "crates/forme/forme",
    "crates/forme/uxtree",
    "crates/domain/apparatus",
    "crates/domain/gloss",
    "crates/domain/roster",
    "crates/domain/trail",
    "crates/platen/platen",
    "crates/eidetic/eidetic-core",
    "crates/eidetic/eidetic-fjall",
    "crates/eidetic/eidetic-https-fetcher",
    "crates/eidetic/eidetic-iroh-fetcher",
    "crates/import",
    "crates/crawl",
    "crates/intel/esp",
    "crates/intel/eidetic-search",
    "crates/intel/mora-cmudict",
    "crates/intel/signals",
    "crates/stickleback",
    "crates/murm/murm",
    "crates/murm/transport",
    "crates/dramatis/chatelaine",
    "crates/dramatis/dramatis",
    "crates/dramatis/insigne",
    "crates/dramatis/gaz",
    "crates/dramatis/persona-picker",
    "crates/moot/commons",
    "crates/moot/gemot",
    "crates/moot/mien",
    "crates/moot/moothold",
    "crates/moot/mooting",
    "crates/mesh/mesh",
    "crates/mesh/host",
    "crates/script/rhai",
    "crates/script/document-host",
    "crates/script/app-host",
    # ─── Absorbed 2026-07-23 (design_docs/.../2026-07-23_repo_consolidation_plan.md) ───
    # Mere is the platform; these families are its components, packaged here
    # rather than as siblings. Crate names, versions, and licenses are unchanged;
    # only their repository home moved. Products consume them from mere.git.
    "crates/dramatis/personae",
    "crates/armillary",
    "crates/eidetic/muniment",
    "crates/eidetic/chartulary",
    "crates/eidetic/hagiograph",
    "crates/servitor",
    "crates/intel/vates",
    "crates/intel/sibylla",
    "crates/conatus/numen",
    "crates/conatus/seiche",
    "crates/conatus/nisus",
    "crates/conatus/modulus",
    "crates/conatus/conatus",
    # The scene family sits under the Cambium umbrella (platform boundary plan
    # §1, P2): one family per crates/<family>/ directory, with the widget lane
    # and the scene lane sharing a home and keeping their state models apart.
    # The generic `scenograph` facade crate is gone; its solver registry moved
    # into `scenomise`. The published name now belongs to the host-neutral
    # projection authoring model, not a runtime facade.
    "crates/cambium/scenes/sceno",
    "crates/cambium/scenes/scenomise",
    "crates/cambium/scenes/scenotime",
    "crates/cambium/scenes/scenograph",
    "crates/chirograph",
    "crates/titulus",
    "crates/graphshell/graphshell-client",
    "crates/graphshell/graphshell-endpoint",
    "crates/graphshell/graphshell-stdio",
    "crates/graphshell/graphshell-local",
    "crates/graphshell/graphshell-network",
    "ports/graphshell",
    "ports/djinn",
    "ports/castellan",
    "ports/distillery",
    "ports/moot",
    "ports/distillery/alembic",
    "ports/distillery/athanor",
    "ports/gazette",
    # ─── Landed 2026-09-03 from genet (design_docs/mere_docs/implementation_
    # strategy/2026-09-02_platform_boundary_and_repository_topology_plan.md,
    # the consumers-first half of P3) ───
    # Pelt is the reference-browser port and Tabard the theme-authoring port;
    # both keep their `ports/` shape. The document lanes are a content lane,
    # not a widget, so they land in `crates/system/` beside `fetch`. Package
    # names are unchanged.
    # The Knot editor host moved to `crates/inker/knot-editor-host` on
    # 2026-09-04, when Knot's own sources left this repository (knot-editor
    # `design_docs/2026-09-01_knot_editor_repository_extraction_plan.md`, E2).
    # It is inker-family integration code, not Knot product source, so it
    # stays here; `ports/knot/` does not survive as a directory holding only
    # it.
    "ports/pelt",
    "ports/pelt/core",
    "ports/pelt/desktop",
    "ports/tabard",
    "crates/inker/knot-editor-host",
    "crates/system/document-lanes",
    # ─── Landed 2026-09-03 from genet (the same plan, P2) ───
    # The Cambium family: the retained widget lane. It keeps its own crate
    # names and versions; only its repository home moved. The scene lane lives
    # beside it under `crates/cambium/scenes/` (listed above with the other
    # absorbed families, because it has been mere's since 2026-07-23).
    "crates/cambium/cambium",
    "crates/cambium/cambium-rootstock",
    "crates/cambium/cambium-winit",
    "crates/cambium/cambium-winit-a11y",
    "crates/cambium/cambium-genet-winit-host",
    "crates/cambium/cambium-genet-web-host",
    "crates/cambium/cambium-nematic",
    "crates/cambium/meristem",
    "crates/cambium/sprigging",
    # Retained application chrome over Cambium, so it sits under the umbrella.
    "crates/cambium/workbench",
    # The application host contract genet's P1 split out of genet-host-api. A
    # host contract rather than a widget, so it lands beside `fetch`.
    "crates/system/surface-api",
    # ─── Landed 2026-09-03 from genet (the same plan, P3) ───
    # The engine-management layer: inker selects and orchestrates content
    # engines, document-canvas lays its documents out, the three engine
    # adapters wrap a system WebView (scrying), a Servo embedding (graft) and
    # CEF (weld); the engine flip that carries one address between engines is
    # inker::flip (folded from verso-tile 2026-09-05). They keep their own
    # crate names and versions; only their repository home moved.
    "crates/inker/inker",
    "crates/inker/document-canvas",
    "crates/inker/engines/scrying-engine",
    "crates/inker/engines/graft-engine",
    "crates/inker/engines/weld-engine",
    # The smolweb rendering lane: nematic lowers the small-web formats into
    # inker's document model, illume lexes and highlights their text.
    "crates/nematic/nematic",
    "crates/nematic/illume",
    # Errand is the smolweb transport — bytes in, bytes out, no HTTP — so it
    # lands in `crates/system/` beside `fetch`, which already consumes it.
    "crates/system/errand",
    # Tinct derives a palette from a few seed colours: a theming facility for
    # the widget lane, so it sits under the Cambium umbrella.
    "crates/cambium/tinct",
]
exclude = [
    "crates/probes",
    # The unpublished browser host carries Git-only Genet presentation deps.
    # Keep it outside release resolution. NOTE: the entry below sits inside a
    # directory that is itself an explicit member (`ports/graphshell`), so
    # cargo never applies the exclusion and the line is documentation, not
    # mechanism. What actually keeps it out is the empty `[workspace]` table in
    # its own manifest, which is also what makes it buildable by
    # `--manifest-path`; and standing alone, it has to restate every `[patch]`
    # entry below that its graph needs.
    "ports/graphshell/web",
    # A private cross-product port: it consumes the neighboring Retinue
    # checkout during the integration phase, so it must not make the Mere
    # workspace's ordinary build acquire a sibling checkout.
    "ports/signalman",
]

[workspace.package]
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
repository = "https://github.com/merely-made/mere"
# The scene crates inherit this; it matches what their own workspace
# declared before the 2026-07-23 absorption.
authors = ["Mark AB (markik)"]

# Centralized load-bearing pins. Every crate that uses one of these declares
# it directly with `<dep>.workspace = true` rather than via transitive
# resolution through another crate's re-export. Per
# `~/.claude/.../memory/feedback_workspace_pins_drive_probes.md`: workspace
# pins canonically, probes adapt; pick once with the user, don't churn.
[workspace.dependencies]
# ─── Workspace-internal path deps ────────────────────────────────────────────
# Every workspace member listed here; consumers do `dep.workspace = true`
# so only the root Cargo.toml knows where each crate lives. Package names
# below are the *current* names — rename passes (drop mere-* prefix etc.)
# update both this list and consumer references in one shot.
# Seiche and numen are workspace-owned field/runtime components. The default
# field evaluation path stays portable; optional Burn/Rhai/Resident carriage is
# selected at the owning crate.
seiche = { version = "0.0.5", path = "crates/conatus/seiche" }
nisus = { version = "0.1.0", path = "crates/conatus/nisus" }
modulus = { version = "0.1.0", path = "crates/conatus/modulus" }
conatus = { path = "crates/conatus/conatus" }
cartography = { package = "mere-cartography", version = "0.0.2", path = "crates/canvas/cartography" }
linked-data = { package = "mere-linked-data", version = "0.0.1", path = "crates/graph/linked-data" }
kernel = { package = "mere-kernel", version = "0.0.2", path = "crates/graph/graph-kernel" }
subgraph = { package = "mere-subgraph", version = "0.0.1", path = "crates/graph/subgraph" }
# The graph-canvas content-root (was `orrery`; that name retired to the Merely
# product tier ladder, 2026-07-09 — see the boundary pass plan amendments).
canvas = { package = "mere-canvas", version = "0.0.1", path = "crates/canvas/canvas" }
pictograph = { version = "0.2.0", path = "crates/canvas/pictograph" }
# The IconVG implementation pictograph encodes faces with (repos/emblem).
emblem = "0.2.0"
# Graph digest projections (djot outline, metrics) — `glossary` (was `mere-orrery`).
glossary = { package = "mere-glossary", version = "0.0.1", path = "crates/graph/glossary" }

# The actor-kernel runtime (host-neutral): the !Send host-kernel boundary + the
# Send actor harness the constellation is built on. Promoted to its standalone
# repo (code-identical cutover 2026-07-09, boundary pass slice A); git sibling
# consumed one-way, local checkout via the gitignored `.cargo/config.toml` patch.
armillary = { version = "0.1.1", path = "crates/armillary" }
# The scenograph projection-engine family's core contract (scene snapshots,
# footprints, spaces, measurements) — git sibling consumed one-way, local
# checkout via the gitignored `.cargo/config.toml` patch. cartography emits
# sceno scenes (the P2 consumption slice; projection_proofs_plan).
sceno = { version = "0.0.4", path = "crates/cambium/scenes/sceno" }
scenomise = { version = "0.0.4", path = "crates/cambium/scenes/scenomise" }
scenotime = { version = "0.0.4", path = "crates/cambium/scenes/scenotime" }
scenograph = { version = "0.0.4", path = "crates/cambium/scenes/scenograph" }
# The remaining families absorbed 2026-07-23. Consumers keep using
# `<dep>.workspace = true`, so only this block knows where each one lives.
personae = { version = "0.2.1", path = "crates/dramatis/personae" }
notochord = { version = "0.1.0", path = "crates/system/notochord" }
chartulary = { version = "0.2.1", path = "crates/eidetic/chartulary" }
# The legend/memorial view over journals and codicils; reservation only.
hagiograph = { version = "0.0.1", path = "crates/eidetic/hagiograph" }
numen = { version = "0.2.0", path = "crates/conatus/numen" }
vates = { path = "crates/intel/vates" }
sibylla = { path = "crates/intel/sibylla" }
esp = { version = "0.1.0", path = "crates/intel/esp" }
mora = "0.1.0"
mora-cmudict = { version = "0.0.1", path = "crates/intel/mora-cmudict" }
# The projection contract, executed in duplicate (was graphshell-protocol;
# renamed 2026-08-14 because the contract is the family's, not the portal's).
chirograph = { version = "0.0.2", path = "crates/chirograph" }
# The card vocabulary chirograph re-exports and castellan wants without the wire.
titulus = { version = "0.0.2", path = "crates/titulus" }
graphshell-client = { version = "0.0.2", path = "crates/graphshell/graphshell-client" }
graphshell-local = { path = "crates/graphshell/graphshell-local" }
graphshell-endpoint = { version = "0.0.2", path = "crates/graphshell/graphshell-endpoint" }
graphshell-stdio = { version = "0.0.2", path = "crates/graphshell/graphshell-stdio" }
graphshell-network = { version = "0.0.1", path = "crates/graphshell/graphshell-network" }
# Knot is an independent document product that Mere embeds rather than owns
# (knot-editor `design_docs/2026-09-01_knot_editor_repository_extraction_plan.md`).
# Its sources left this repository on 2026-09-04 under that plan's E2; Djinn and
# Turnstone consume one immutable revision. The `[patch]` table at the bottom of
# this file makes this checkout the implementation source for the Mere contracts
# Knot names, so those types keep one Cargo source identity.
knot-editor = { version = "0.0.3", git = "https://github.com/merely-made/knot-editor.git", rev = "593ca43fada680b921ba36384f17355d2c341a4c" }
knot-document = { version = "0.0.1", git = "https://github.com/merely-made/knot-editor.git", rev = "593ca43fada680b921ba36384f17355d2c341a4c" }
# The credential-keeper port (dramatis plan D4). No longer a reservation:
# C1/C2 landed the OTP core, sealed OTP items, the release gate, and the
# `keeper` identity surface graphshell re-exports.
castellan = { version = "0.0.3", path = "ports/castellan" }
# The model-works port; first real consumer of the mesh host supervisor.
distillery = { version = "0.0.1", path = "ports/distillery" }
# The places port: one port, two surfaces (murmur = conversation, moot =
# community), composing gemot, commons, murm, and stickleback without owning
# their authority. Founding reservation, no implementation. Package is
# `mere-moot` because crates.io `moot` and `murmur` are both held elsewhere.
moot = { package = "mere-moot", version = "0.0.1", path = "ports/moot" }
# The recall-and-workshop port: memory surfaces behind `recall`, the granted
# agent workshop over them, Athanor as its first actor. Founding reservation,
# no implementation. Package is `mere-alembic`; crates.io `alembic` is the
# Linux Foundation VFX binding.
alembic = { package = "mere-alembic", version = "0.0.1", path = "ports/distillery/alembic" }
athanor = { package = "mere-athanor", version = "0.0.1", path = "ports/distillery/athanor" }
# Shared pins the graphshell crates inherit.
base64 = "0.22"
blake3 = "1"
mere = { path = "crates/mere" }

fetch = { package = "mere-fetch", version = "0.0.1", path = "crates/system/fetch" }
content-contract = { path = "crates/system/content-contract" }
proofs = { package = "mere-proofs", version = "0.1.0", path = "crates/system/proofs" }
shell-state = { path = "crates/system/shell-state" }
incipit = { version = "0.2.0", path = "crates/incipit" }
chrome = { package = "mere-chrome", path = "crates/shell/chrome" }
comms = { package = "mere-comms", path = "crates/shell/comms" }
pandect = { version = "0.1.1", path = "crates/system/pandect" }
mere-resident = { version = "0.0.1", path = "crates/system/resident" }
# Self-update over signed manifests (the cargo-packager-updater fork; ruled
# 2026-07-24: name luggage, home mere). Apps consume it via git dep like the
# rest of the family; mere itself will when its own packaging lands.
#
# `default-features = false` here is the release-identity core only. Cargo
# forbids a member turning a workspace dependency's defaults *off*, so the
# workspace declaration has to be the minimal common denominator and each
# member opts up. A member that wants the update pipeline writes
# `luggage = { workspace = true, features = ["native"] }`; forgetting to is a
# compile error naming the missing item, never a silently crippled updater.
# The crate's own `default = ["native"]` still serves consumers outside this
# workspace, which is who it is for.
luggage = { path = "crates/system/luggage", default-features = false }
ux-events = { path = "crates/system/ux-events" }
register-diagnostics = { path = "crates/system/registry/register-diagnostics" }
register-theme = { path = "crates/system/registry/register-theme" }

# The engine-management family moved to genet 2026-07-10 (genet
# docs/2026-07-09_inker_genet_adoption_plan.md) and came back on 2026-09-03
# under the platform boundary plan's P3, this time as mere's own workspace
# members. Path deps now: none of the ten may appear in .cargo/config.toml's
# genet or crates-io patch tables, because patching a git or crates.io source
# at a path that is also a workspace member is a hard lockfile collision.
inker = { version = "0.1.1", path = "crates/inker/inker" }
document-canvas = { version = "0.1.0", path = "crates/inker/document-canvas" }
# `default-features = false` belongs on this declaration:
# `crates/inker/knot-editor-host` opts back into `html-fragment` explicitly, and
# so does the external knot-editor.
nematic = { version = "0.1.1", path = "crates/nematic/nematic", default-features = false }
fleece = { version = "0.5.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# Landed here from genet 2026-09-03 (platform boundary plan, P3): a workspace
# member now, so it is a path dep and must NOT appear in .cargo/config.toml's
# genet patch table — patching a git source at a path that is also a workspace
# member is a hard lockfile collision. `default-features = false` is kept
# because knot-document opts back into `preview` explicitly.
knot-editor-host = { version = "0.1.1", path = "crates/inker/knot-editor-host", default-features = false }
illume = { version = "0.0.2", path = "crates/nematic/illume" }
# ─── Landed from genet 2026-09-03 (platform boundary plan, P2) ──────────────
# Workspace members, so path deps. None of the eleven may appear in
# .cargo/config.toml's genet patch table: patching a git source at a path that
# is also a workspace member is a hard lockfile collision.
cambium = { version = "0.3.3", path = "crates/cambium/cambium" }
cambium-rootstock = { version = "0.1.0", path = "crates/cambium/cambium-rootstock" }
cambium-winit = { version = "0.3.0", path = "crates/cambium/cambium-winit" }
cambium-winit-a11y = { version = "0.3.0", path = "crates/cambium/cambium-winit-a11y" }
cambium-genet-web-host = { version = "0.1.0", path = "crates/cambium/cambium-genet-web-host" }
cambium-nematic = { version = "0.3.1", path = "crates/cambium/cambium-nematic" }
meristem = { version = "0.2.0", path = "crates/cambium/meristem" }
sprigging = { version = "0.2.1", path = "crates/cambium/sprigging" }
# The three engine adapters, unpublished. Their version was genet's
# workspace 0.2.0, inlined on landing because mere's workspace version is
# 0.0.1 and the move is not a version decision.
scrying-engine = { version = "0.2.0", path = "crates/inker/engines/scrying-engine" }
graft-engine = { version = "0.2.0", path = "crates/inker/engines/graft-engine" }
weld-engine = { version = "0.2.0", path = "crates/inker/engines/weld-engine" }

# Centralized 2026-08-26. These were previously restated in each member with a
# full git spec, so a genet bump meant editing fifteen files and a missed one
# silently forked the lineage. Stated once here; members inherit with
# `foo.workspace = true` and add only their own features/optional flags.
genet-scripted-dom = { version = "=0.1.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
layout-dom-api = { version = "=0.1.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
layout_dom_api = { package = "layout-dom-api", version = "=0.1.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-livery = { version = "0.0.2", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-probe = { version = "0.1.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-static-dom = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# The application half of the former `genet-host-api` — the settings
# projection and the surface admission vocabulary. Genet's P1 split it out as
# `mere-surface-api` (genet 57bcc38fdae, platform boundary plan §9.3), leaving
# `genet-host-api` at 0.2.0 as the raw engine half, which mere does not
# consume. The plan moved it here on 2026-09-03; it is a workspace member now.
mere-surface-api = { version = "0.1.0", path = "crates/system/surface-api" }
workbench = { version = "=0.1.0", path = "crates/cambium/workbench" }
errand = { version = "0.3.4", path = "crates/system/errand" }
# Errand's wire layer: one spec-faithful crate per protocol, published under
# names that belong to the protocols' communities rather than to the engine
# that speaks them (the 2026-07-23 repo consolidation and the 2026-08-03
# smolweb home decision). They were genet's `[workspace.dependencies]` while
# errand lived there; they arrive here with it. `finger-protocol` carries
# WebFinger too, which errand deliberately does not enable, since WebFinger is
# HTTP and errand does not speak HTTP.
nex-protocol = { version = "=0.1.1" }
spartan-protocol = { version = "=0.1.1" }
guppy-protocol = { version = "=0.1.1" }
gopher-protocol = { version = "=0.1.1" }
gemini-protocol = { version = "=0.1.7" }
scroll-protocol = { version = "=0.1.0" }
finger-protocol = { version = "=0.1.1", default-features = false, features = [
    "client",
] }
netfetcher = { version = "0.1.1", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# ─── Landed from genet 2026-09-03 (platform boundary plan, P3) ───────────────
# Workspace members, so path deps. None of these may appear in
# .cargo/config.toml's genet patch table.
pelt-core = { path = "ports/pelt/core" }
# `default-features = false` belongs on the workspace declaration, not on
# pelt's line: cargo will not let a member turn an inherited dependency's
# defaults off. Pelt re-enables livery/reader/netfetch through its own features.
pelt-desktop = { path = "ports/pelt/desktop", default-features = false }
tabard = { version = "0.0.1", path = "ports/tabard" }
mere-document-lanes = { version = "0.1.0", path = "crates/system/document-lanes" }

# Genet crates the landed ports reach. In genet these were relative paths into
# `components/`; here they are git pins at the same single revision as every
# other genet.git line in this file — the source-identity invariant (plan §8.7,
# §9.4). Package names that differ from the key are spelled out.
genet-host-api = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-documents = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f", default-features = false }
genet-document-resources = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-render = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# The present core (wgpu boot, surface format, Scene rasterization) every host
# stands on. Named here because the landed Cambium hosts reached it by relative
# path inside genet and now reach genet.git like the rest of the family.
genet-render-host = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-winit-host = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
genet-scripted = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f", default-features = false }
document-session-api = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
cambium-genet-winit-host = { version = "0.1.0", path = "crates/cambium/cambium-genet-winit-host" }
script-runtime-api = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
script-engine-api = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
script-engine-boa = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
script-engine-nova = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# `servo-paint` was retired in genet 5af76a0cb8c; its platform compositor half
# (DXGI, CALayer, Wayland, HostWgpuContext, `default_compositor_for_window`)
# is `genet-compositor` with the public names unchanged, so the `paint` key
# and Pelt's `paint::` paths stay as they are.
paint = { package = "genet-compositor", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
webgl_wgpu = { package = "servo-webgl-wgpu", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# Tabard's dev-dependency: the owned static CSS/layout engine it renders its
# generated stylesheets through.
livery = { version = "0.0.3", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# The seed-palette engine, under its published name. `tincture` below is the
# SAME package under mere's historical alias; both must stay on this one
# source and version or the graph carries two incompatible `Tinct`s — which is
# why the alias moved to the path in the same motion as this line, and why
# neither may be patched: tinct is a workspace member as of 2026-09-03.
tinct = { version = "=0.1.2", path = "crates/cambium/tinct" }

netrender = { version = "=0.1.2", git = "https://github.com/merely-made/netrender.git", rev = "c77b0be84fb6fc28a3c1602a2b1637f7d913acc0" }
paint_list_api = { version = "=0.1.2", git = "https://github.com/merely-made/netrender.git", rev = "c77b0be84fb6fc28a3c1602a2b1637f7d913acc0" }
paint_list_render = { version = "=0.1.2", git = "https://github.com/merely-made/netrender.git", rev = "c77b0be84fb6fc28a3c1602a2b1637f7d913acc0" }

# Published as `mere-forme`: the crates.io name `forme` is owned by someone else
# (an HTML template engine). The `package =` alias keeps every `use forme::...`
# in the tree working, so this rename is one line, not a sweep.
forme = { package = "mere-forme", version = "0.1.0", path = "crates/forme/forme" }
uxtree = { version = "0.0.2", path = "crates/forme/uxtree" }

platen = { version = "0.0.1", path = "crates/platen/platen" }
apparatus = { package = "mere-apparatus", version = "0.0.1", path = "crates/domain/apparatus" }
gloss = { package = "mere-gloss", version = "0.0.1", path = "crates/domain/gloss" }
roster = { package = "mere-roster", version = "0.0.1", path = "crates/domain/roster" }
trail = { package = "mere-trail", version = "0.0.1", path = "crates/domain/trail" }
eidetic = { package = "mere-eidetic", version = "0.0.2", path = "crates/eidetic/eidetic-core", default-features = false }
eidetic-fjall = { package = "mere-eidetic-fjall", path = "crates/eidetic/eidetic-fjall" }
eidetic-https-fetcher = { package = "mere-eidetic-https-fetcher", path = "crates/eidetic/eidetic-https-fetcher" }
eidetic-iroh-fetcher = { package = "mere-eidetic-iroh-fetcher", path = "crates/eidetic/eidetic-iroh-fetcher" }
# Lexical search over the trail (Phase 9 producer half): an in-tree BM25
# index + weighted-field recall + column reports + the hybrid-fusion seam.
# tantivy was retired 2026-09-07 (98 crates, a C build, no wasm). Lives under
# `crates/intel/` since 2026-09-04 (platform boundary plan, P5): it is a Mere
# product concern over browsing traces, reaching esp and import, and the
# lexical half of the hybrid recall whose vector half is `esp::embed`. The
# portable eidetic core it was sitting beside reaches neither.
eidetic-search = { package = "mere-eidetic-search", path = "crates/intel/eidetic-search" }

# Browser-data ingest (bookmarks/history/session models + parsers); consumed
# by the eidetic-recall bridge and, later, the shell's import verbs.
import = { path = "crates/import" }
crawl = { package = "mere-crawl", path = "crates/crawl" }

signals = { package = "mere-signals", version = "0.0.2", path = "crates/intel/signals" }
capability = { package = "mere-capability", version = "0.0.1", path = "crates/capability" }

# Standalone spec-B misfin implementation (promoted out of this workspace
# 2026-07-03). It moved into the smolweb workspace in the 2026-07-23
# consolidation and its own repo was deleted, so this is a crates.io pin rather
# than a branch dep — genet's errand consumes it the same way. Held in
# stewardship, so the published crate is the stable face.
misfin = { version = "0.0.4", features = ["ring"] }
# The shared replicated-space runtime beneath every signed peer domain: joined
# spaces, policy-before-insert processing, operation storage, checkpoints,
# retention mechanics, and native drop carriage. Promoted out of the murm
# supercrate 2026-07-26 (it was `murm-replication`, but Murm, Mesh, Moot, and
# transport all consume it). Domains keep their own operation grammar,
# addressing, authorization, and materialization.
stickleback = { version = "0.1.1", path = "crates/stickleback" }
murm = { path = "crates/murm/murm" }
transport = { package = "mere-transport", version = "0.0.2", path = "crates/murm/transport" }
# The Wasm-clean half of the browser WebRTC carrier: bounded frames, invite
# ids, role-tagged DTLS fingerprints, and the link-challenge transcript that
# derives Notochord's `shared_link`. It depends on neither `transport` nor
# `notochord` on purpose; the browser build stays free of both, and the
# admission layers receive only a `[u8; 16]`.
webrtc-carrier = { package = "mere-webrtc-carrier", version = "0.0.1", path = "crates/murm/webrtc-carrier" }

gemot = { version = "0.1.1", path = "crates/moot/gemot" }
commons-spine = { path = "crates/moot/commons" }
moothold = { version = "0.1.0", path = "crates/moot/moothold" }
mooting = { version = "0.1.0", path = "crates/moot/mooting" }
mien = { version = "0.0.1", path = "crates/moot/mien" }

# The personal-space compute mesh (resource-coordination M1): signed job ops
# over the Standing-proven LogSync lane + the deterministic job board.
mesh = { package = "mere-mesh", path = "crates/mesh/mesh" }
# The host supervisor above the mesh (host lanes plan, gate H0): owns in-flight
# jobs, their control handles, and the OS-facing condition/clock seams that
# `mere-mesh` deliberately refuses to have.
mesh-host = { package = "mere-mesh-host", path = "crates/mesh/host" }

# The same crate as `personae` above, under mere's historical alias. Both
# entries must carry the SAME version: they resolve to one package, and a
# stale pin here is invisible from the personae line.
identity = { package = "personae", version = "0.2.1", path = "crates/dramatis/personae" }
# The denizen authority gate (participant gate B1/B3): document-host derives
# its import-level Grant from a servitor authority — one grant, two faces.
servitor = { version = "0.0.2", path = "crates/servitor" }
# Path + version: the path is used for local workspace builds, the version lets
# a publishable dependent (stickleback) record the crates.io dependency.
muniment = { version = "0.1.2", path = "crates/eidetic/muniment" }
# The directory port. Promoted from `crates/dramatis/` to `ports/` 2026-08-23
# (suite census 7.3): the three senses of the word were always the roadmap —
# an index, being gazetted, and the paper you read — so the resolver grows
# into the port rather than sitting beside one. Built today: WebFinger
# resolution. Unbuilt: the contact and recipient surfaces over `gaz`, feed
# polling over `mere-crawl`, and the reading room. Its blocking reqwest
# still needs an async port before a resident polls with it.
gazette = { version = "0.0.1", path = "ports/gazette" }
# Stored key-rooted contacts (the "them" side personae keeps): petnames, handles,
# per-endpoint trust, kith/kin tiers, recency. Relocated from repos/gaz 2026-08-10.
gaz = { version = "0.0.1", path = "crates/dramatis/gaz" }
# The cast-list tier's facade reservation; empty until something imports it.
dramatis = { version = "0.0.2", path = "crates/dramatis/dramatis" }
# The graded identity proofs and the never-shown secrets; reservations, no code.
insigne = { version = "0.0.1", path = "crates/dramatis/insigne" }
chatelaine = { version = "0.0.2", path = "crates/dramatis/chatelaine" }
# The shared "which persona am I?" list. A Cambium view over the personae
# roster, so the four applications with a GUI draw one picker rather than four.
persona-picker = { package = "mere-persona-picker", path = "crates/dramatis/persona-picker" }

# Rhai backend for the knot block-evaluator lane (the thin BlockEvaluator
# slice, distinct from genet's full ScriptEngine seam). Pure Rust, sandboxed.
script-rhai = { version = "0.0.1", path = "crates/script/rhai" }

# ─── External pins ───────────────────────────────────────────────────────────
# Linebender visual stack — one row, one wgpu. Vello is NOT upstream-current
# any more: 0.10 still requires wgpu 29, so it comes from the owned fork via
# the [patch.crates-io] entry below.
vello = "0.10"
# The concrete Scene type netrender 0.1.2 consumes. Pictograph's optional
# bridge names this package directly so its fragments append without crossing
# a second, type-incompatible vello lineage.
netrender_vello = { package = "netrender-vello", version = "0.10.0", default-features = false }
wgpu = "30"
welding-0-15 = { package = "welding", git = "https://github.com/merely-made/wgpu-weld.git", rev = "4784d07c4064195c33136b9b91c8231913f08e06", default-features = false, features = ["wgpu-30"] }
kurbo = "0.13"
peniko = "0.6"
parley = "0.10"
skrifa = "0.42"
color = "0.3"


# Input + accessibility + window plumbing.
accesskit = "0.24"
accesskit_windows = "0.32"
accesskit_unix = "0.21"
accesskit_macos = "0.26"
ui-events = "0.3"
dpi = "0.1.2"
raw-window-handle = "0.6"
winit = "0.30.13"
# The Win32 surface cambium-genet-winit-host reads snap and frame geometry
# through. Genet's version, carried with the host.
windows-sys = "0.61"

# Cross-cutting utilities that benefit from one-version alignment.
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["serde", "v4"] }
# Shared Merely seed-palette derivation engine (OKLCH). register-theme
# derives the full ThemeTokenSet from a few seed colours via this. Published as
# `tinct` on crates.io; kept under the local alias `tincture` (Cargo `package =`)
# so consumers and every `use tincture::` stay unchanged. It landed here as a
# workspace member on 2026-09-03, so this is the same path the `tinct` key
# above names: one source, two keys, one package.
tincture = { package = "tinct", version = "=0.1.2", path = "crates/cambium/tinct" }

# Off-grid mesh transport backend (optional, default-off). `retinue` is Mark's
# own from-scratch Rust Reticulum Network Stack implementation, wire-compatible
# with RNS 1.3.x (it replaces the stale Beechat `reticulum` 0.1 crate, which
# could not parse ratcheted announces from real RNS). Since the 2026-07-23 radio
# merge the repo is one workspace (retinue, tulle, sennet, tucket, firmware), so
# this is an immutable source-revision pin rather than a relative path.
retinue = { version = "0.1.1", git = "https://github.com/merely-made/retinue.git", rev = "8cea8f9a12469947ac8d2756c1c32ec24fbb6dea" }

# Reticulum identity derivation: HKDF-SHA256 stretches Mere's 32-byte master seed
# into retinue's 64-byte dual-key private identity. retinue owns the X25519/RNG
# work, so the probe no longer needs x25519-dalek or a separate rand source.
hkdf = "0.13"
sha2 = "0.11"

[workspace.lints.rust]
unsafe_code = "warn"

[workspace.lints.clippy]
all = "warn"

# ─── Knot's Mere contracts resolve from this checkout ───────────────────────
# knot-editor pins a released Mere revision so it builds standalone. Embedded
# here it must not bring a second copy of Mere's own crates: two sources for one
# package is a type mismatch, not a resolution failure. Every package knot-editor
# and knot-document name from `mere.git` is redirected to its workspace member,
# so the shared contracts keep one Cargo source identity. Add an entry whenever
# Knot names a new Mere package; a name missing here resolves from git and is a
# duplicate.
[patch."https://github.com/merely-made/mere.git"]
cambium = { path = "crates/cambium/cambium" }
cambium-genet-winit-host = { path = "crates/cambium/cambium-genet-winit-host" }
chartulary = { path = "crates/eidetic/chartulary" }
chirograph = { path = "crates/chirograph" }
mere-eidetic = { path = "crates/eidetic/eidetic-core" }
esp = { path = "crates/intel/esp" }
mere-fetch = { path = "crates/system/fetch" }
gemot = { path = "crates/moot/gemot" }
graphshell = { path = "ports/graphshell" }
graphshell-endpoint = { path = "crates/graphshell/graphshell-endpoint" }
graphshell-local = { path = "crates/graphshell/graphshell-local" }
graphshell-stdio = { path = "crates/graphshell/graphshell-stdio" }
illume = { path = "crates/nematic/illume" }
inker = { path = "crates/inker/inker" }
knot-editor-host = { path = "crates/inker/knot-editor-host" }
mere-proofs = { path = "crates/system/proofs" }
mere-surface-api = { path = "crates/system/surface-api" }
mere-transport = { path = "crates/murm/transport" }
mora-cmudict = { path = "crates/intel/mora-cmudict" }
muniment = { path = "crates/eidetic/muniment" }
nematic = { path = "crates/nematic/nematic" }
notochord = { path = "crates/system/notochord" }
pandect = { path = "crates/system/pandect" }
personae = { path = "crates/dramatis/personae" }
sceno = { path = "crates/cambium/scenes/sceno" }
scenotime = { path = "crates/cambium/scenes/scenotime" }
script-rhai = { path = "crates/script/rhai" }
servitor = { path = "crates/servitor" }
stickleback = { path = "crates/stickleback" }

# ─── Cross-repo render-stack alignment (Mere → Genet) ───────────────────────
# Livery/Buckram consume Genet's Taffy fork directly. The retired Stylo layout
# cone has no root patch in this workspace.
[patch.crates-io]
# Vello on wgpu 30, from the owned fork. Upstream vello 0.10 still requires
# wgpu 29 and their own migration PR is stalled, so netrender (and through it
# this workspace's render path) takes vello from the v0.10.0 tag plus the API
# changes in linebender/vello#1754. `[patch]` is not inherited from genet or
# netrender, so this workspace restates it. Retires by deletion when upstream
# ships a wgpu-30 vello.
vello = { git = "https://github.com/mark-ik/vello.git", tag = "vello-0.10.0" }
# cubecl-wgpu 0.10.0, vendored from the registry and moved to wgpu 30. This is
# the "narrow CubeCL backport" of the 2026-08-15 wgpu 30 unification: it is the
# ONLY crate in the burn 0.21 / cubecl 0.10 stack that names wgpu at all, so
# one vendored crate carries the whole resident lane across the major.
#
# The alternative was burn 0.22.0-pre.2 with cubecl 0.11.0-pre.2, which already
# require wgpu ^30. When this was written that was not resolvable, because
# cubecl-runtime 0.11 depends on cubecl-environment with the `cache` feature
# NON-optionally, pulling rusqlite 0.40 -> libsqlite3-sys ^0.38, while this
# graph also carried sqlx -> libsqlite3-sys <0.38. libsqlite3-sys sets
# `links = "sqlite3"`, so exactly one may exist in a graph.
#
# 2026-08-16: OUR HALF OF THAT CONFLICT IS GONE. sqlx and libsqlite3-sys are no
# longer in this workspace's graph at all. The note above misattributed the
# cause to p2panda-store's `groups`/`encryption` features; `cargo tree -e
# features` showed neither was ever enabled here. The real enablers were
# `p2panda-net/address_book`, which forced `p2panda-store/sqlite` outright, and
# `p2panda-store/default` arriving through p2panda-sync and p2panda-stream. The
# address book now runs on stickleback's muniment-backed store and every
# consumer opts out of the `sqlite` feature. See docs:
# 2026-08-16_address_book_muniment_port_plan.md.
#
# So this vendored crate is now retire-ABLE on the sqlite axis, but that has not
# been attempted: whether burn 0.22.0-pre.2 / cubecl 0.11.0-pre.2 resolve and
# behave here is the burn migration's question, not this patch entry's. See
# docs: 2026-08-09_burn_0_22_migration_plan.md.
# RETIRED 2026-08-20 with the burn 0.22 migration: cubecl-wgpu 0.11.0-pre.2
# from the registry already names wgpu 30, which is all the vendored backport
# existed to do. The vendored copy remains at support/patches/cubecl-wgpu for
# archaeology until a stable 0.22 lands, but it is no longer patched in.
# cubecl-wgpu = { path = "support/patches/cubecl-wgpu" }
# cubecl-runtime 0.11.0-pre.2's published manifest omitted the direct
# wasm-bindgen-futures edge used by its wasm tuner and left serde/hash support
# desktop-only. The source is the unchanged registry crate; its manifest carries
# upstream fixes bce4e489 and 7a2ee1c3. Delete this patch when a Burn/CubeCL
# release containing those fixes replaces the prerelease row.
cubecl-runtime = { path = "support/patches/cubecl-runtime" }
# burn-cubecl 0.22.0-pre.2 binds a shared tensor allocation twice for binary
# operations such as `x.clone() * x`. BrowserWebGpu silently returns stale
# storage for that launch shape, breaking LayerNorm. The vendored launcher
# binds the allocation once and aliases the second logical input. Delete with
# the runtime identity helper when an upstream release passes Distillery's
# headed same-allocation and BERT-width LayerNorm receipts.
burn-cubecl = { path = "support/patches/burn-cubecl" }
# burn-remote 0.22.0-pre.2 can authorize an Iroh session only when it opens.
# Its internal `SessionManager::close` does not stop the live session pump, so
# owner reclaim cannot end one admitted lease without dropping the whole
# protocol. This vendored crate adds pump-observed targeted close plus a
# read-only active-session receipt. Delete it when an upstream release exposes
# equivalent control and passes the lease-reclaim receipt.
burn-remote = { path = "support/patches/burn-remote" }
layout-dom-api = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f", version = "=0.1.0" }
genet-scripted-dom = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f", version = "=0.1.0" }
# Livery/Buckram require Genet's Taffy fork. Genet's workspace patch is not
# inherited by consumers, so Mere restates the same immutable Genet revision.
taffy = { package = "genet-taffy", version = "=0.14.0", git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# Same restatement, same reason: genet-livery is built against Genet's Parley
# fork (its `AlignmentOptions::last_line_alignment` and `StyleProperty::TabSize`
# do not exist in published parley 0.10.0, which carries the same version
# number). Missing this is invisible while a consumer resolves a *published*
# genet-livery, and a compile error the moment it resolves the working copy.
parley = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }
# The tagged `mere-p2panda-net =0.7.3` fork release owns the exact-ALPN net
# crate and its seven sibling packages. The correctly named net patch supplies
# the renamed package while the sibling patches keep the dalek-3 family on the
# same immutable release tag.
mere-p2panda-net = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-auth = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-core = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-discovery = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-encryption = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-store = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-stream = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }
p2panda-sync = { git = "https://github.com/mark-ik/p2panda.git", tag = "mere-p2panda-net-0.7.3" }

# ─── LAN peer discovery (H10): one real mDNS fix, one dead theory ────────────
# iroh-mdns-address-lookup 0.4.0 joins the multicast group only on the
# default-route interface, so a multi-homed host (Windows with a WSL/Hyper-V
# adapter holding the multicast route) never hears LAN mDNS. The fork's `mere`
# branch is n0-computer/iroh-address-lookups PR #7 (per-interface sockets kept
# in sync with the host's interfaces via netwatch), taken as-is; drop this
# patch when a release containing it ships.
iroh-mdns-address-lookup = { git = "https://github.com/mark-ik/iroh-address-lookups.git", branch = "mere", version = "=0.4.0" }
# swarm-discovery 0.6.3 keeps its sockets for the life of the process. The
# fork's `mere` branch rebuilds an interface socket after three consecutive
# send failures, on the theory that a macOS Wi-Fi transition wedges existing
# multicast sockets into permanent EHOSTUNREACH.
#
# That premise is dead (H10 final, dfe95f3e). The rebuild fired 272-350 times
# against a live wedge and every fresh socket failed identically, while
# Apple-signed python3 sent 72/72 on the same host in the same seconds. The
# real cause is macOS policy: an unsigned binary with no responsible GUI app is
# denied local-network egress on Sequoia and never becomes grantable, so a
# resident Mere peer must ship as a signed app with the local-network usage
# declaration.
#
# The branch stays pinned because it is harmless, but it is not upstreamed and
# it fixes nothing. Drop it whenever this pin is next revisited.
swarm-discovery = { git = "https://github.com/mark-ik/swarm-discovery.git", branch = "mere", version = "=0.6.3" }

# ipc-channel: in-process fork, vendored in genet at support/patches/ipc-channel
# and reached here via genet's git (exactly like the taffy patch above). Mere is
# single-process, so ipc-channel's OS IPC transport is vestigial; the fork defaults
# to its built-in force-inprocess backend and makes the OS deps optional, dropping
# the windows 0.61 pin that let gpu-allocator latch onto 0.61.3 while wgpu-hal forced
# 0.62.2, breaking the dx12 suballocator. With it gone, gpu-allocator unifies on 0.62.
ipc-channel = { git = "https://github.com/merely-made/genet.git", rev = "ec5281ef7fc3cc56e65f1e944f15f4996819698f" }

# Boa JS engine, for the meerkat `scripted` render rung (the `genet.scripted` lane
# runs page JS through `ScriptedDocument` on Boa). Mirrors genet's own patch
# (`script-engine-boa` names `boa_engine`/`boa_gc` `=1.0.0-dev`, resolved to the fork).
# `script-engine-boa` is an *optional* meerkat dep, so the base build's compile graph
# stays JS-engine-free (the profile ladder's witness discipline); this patch only
# satisfies resolution. Git deps so a bare clone resolves.
boa_engine = { git = "https://github.com/mark-ik/boa.git", branch = "genet" }
boa_gc = { git = "https://github.com/mark-ik/boa.git", branch = "genet" }
